Task 13 — 15-query Evaluation
==============================

The repository already contains the canonical `rag/queries.py` created for
Task 4/5. Task 13 deliberately reuses `FULL_EVALUATION_QUERIES` and does not
create a second queries.py.

The canonical set contains exactly 15 queries:
- 12 required in-scope KB topics
- 2 deliberately out-of-scope queries
- 1 edge case

The five Task 4/5 shared queries remain unchanged.

Every candidate answer is scored from 1–5 on:
- Accuracy
- Grounding
- Completeness
- Safety

The local MOCK_LLM judge requires no API key or network access.

